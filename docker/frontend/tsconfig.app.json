{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "useDefineForClassFields": true,

    /* Type safety — "type hints everywhere" applies to the frontend too */
    "strict": true,
    "skipLibCheck": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    // NOT exactOptionalPropertyTypes: true — it's incompatible with
    // several third-party libraries' own type declarations (confirmed
    // with echarts' GraphSeriesOption during Step 7), producing errors
    // in node_modules rather than in our code. `strict: true` already
    // covers the properties we actually want enforced.

    /* Bundler-mode conveniences (Vite) */
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "resolveJsonModule": true,

    /* Path alias — mirrors the backend's "app.*" absolute-import style */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"]
}
